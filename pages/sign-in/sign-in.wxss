/*
每日签到页面 - 签到日历模块

上游依赖：sign-in.wxml结构文件
入口：页面加载时自动应用样式
主要功能：定义签到日历界面的视觉样式，包括签到卡片、日历网格、提示区域等组件
输出：渲染美观的每日签到界面样式

重要：每当所属的代码发生变化时，必须对相应的文档进行更新操作！
*/
/* pages/sign-in/sign-in.wxss */
.sign-in-container {
  min-height: 100vh;
  background: linear-gradient(180deg, #f0f9ff 0%, #e0f2fe 100%);
  padding-bottom: 40rpx;
}

/* 顶部导航 */
.page-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: calc(20rpx + env(safe-area-inset-top)) 40rpx 20rpx 40rpx;
  background: white;
  border-bottom: 2rpx solid #e0f2fe;
  position: sticky;
  top: 0;
  z-index: 100;
}

.header-left,
.header-right {
  width: 80rpx;
}

.back-icon {
  font-size: 64rpx;
  color: #0ea5e9;
  line-height: 1;
}

.header-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #1e293b;
}

/* 签到信息卡 */
.sign-card {
  margin: 40rpx;
  background: white;
  border-radius: 32rpx;
  padding: 36rpx 40rpx;
  box-shadow: 0 16rpx 48rpx rgba(14, 165, 233, 0.12);
}

.sign-main {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16rpx;
}

.sign-days-value {
  font-size: 52rpx;
  font-weight: 700;
  color: #0ea5e9;
  line-height: 1;
}

.sign-days-label {
  font-size: 24rpx;
  color: #64748b;
  margin-top: 8rpx;
}

.sign-status {
  font-size: 24rpx;
  padding: 8rpx 20rpx;
  border-radius: 999rpx;
  font-weight: 500;
}

.sign-status.signed {
  color: #10b981;
  background: #d1fae5;
}

.sign-status.unsigned {
  color: #ef4444;
  background: #fee2e2;
}

.sign-points {
  font-size: 26rpx;
  color: #475569;
  margin-bottom: 24rpx;
}

.sign-btn {
  width: 100%;
  padding: 20rpx 0;
  font-size: 28rpx;
  color: white;
  background: linear-gradient(135deg, #0ea5e9 0%, #06b6d4 100%);
  border-radius: 32rpx;
  border: none;
  font-weight: 600;
}

.sign-btn::after {
  border: none;
}

.sign-btn.disabled {
  background: #e2e8f0;
  color: #94a3b8;
}

/* 签到日历 */
.calendar-card {
  margin: 0 40rpx 40rpx;
  background: white;
  border-radius: 32rpx;
  padding: 32rpx;
  box-shadow: 0 12rpx 36rpx rgba(15, 23, 42, 0.08);
}

.calendar-header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin-bottom: 20rpx;
}

.calendar-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #1e293b;
}

.calendar-subtitle {
  font-size: 24rpx;
  color: #94a3b8;
}

.calendar-weekdays {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  font-size: 24rpx;
  color: #64748b;
  text-align: center;
  margin-bottom: 12rpx;
}

.calendar-days {
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 8rpx;
}

.day-cell {
  height: 72rpx;
  border-radius: 16rpx;
  background: #f8fafc;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 26rpx;
  color: #475569;
}

.day-cell.empty {
  background: transparent;
}

.day-cell.today {
  border: 2rpx solid #0ea5e9;
  color: #0ea5e9;
  background: #e0f2fe;
}

.day-cell.signed {
  background: #dcfce7;
  color: #16a34a;
}

.day-cell.today.signed {
  background: #bbf7d0;
  border-color: #16a34a;
  color: #16a34a;
}

.calendar-legend {
  display: flex;
  gap: 24rpx;
  margin-top: 20rpx;
  font-size: 24rpx;
  color: #64748b;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.legend-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: #e2e8f0;
}

.legend-dot.signed {
  background: #22c55e;
}

.legend-dot.today {
  background: #0ea5e9;
}
